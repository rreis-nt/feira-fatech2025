##SALVAR ARQUIVO

cd "c:\SITE FATECH"

# 1) ver o que mudou
git status

# 2) preparar e gravar (commit) com mensagem curta
git add .
git commit -m "Descrição curta do que foi alterado"

# 3) integrar alterações remotas (evita rejeição no push)
git pull --rebase origin main

# 4) enviar para o GitHub
git push origin main